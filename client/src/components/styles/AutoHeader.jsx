import styled from '@emotion/styled';
import {AccentColor} from '../../styles/colors';

const StyledAutoHeader = styled.header`
    padding-left: 10%
    padding-right: 10%
    width: 100%;
    background: ${AccentColor};
    opacity: 0.98;
    display: flex;
    height: 40px;
`;

export default StyledAutoHeader;